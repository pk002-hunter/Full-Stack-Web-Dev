Name: Member 2
Role: Backend Developer (Database & API)

--- Past (What Was Done) ---
- Core Database Architecture: Designed the Django models to migrate volatile field data to a persistent and reliable SQLite database.
- RESTful API Development: Developed the necessary API endpoints to handle data submissions from the casualty cards and serve aggregated data to the Dashboard.
- Data Transformation Pipeline: Implemented the logic to merge real-time vitals from the Node.js backend with persistent medical history stored in Django.
- Triage Logic Implementation: Structured data models for triage categories, patient vitals, and injury reports to ensure robust data integrity.

--- Present (Current Impact & Why It Is Important) ---
- Data Reliability: By persisting data in SQLite, the system ensures that critical medical history is never lost during power cycles or network failures.
- Unified Data Access: The `api_soldiers_view` provides a single, consistent endpoint for all frontend components to receive the latest patient status.
- System Scalability: The modular API design allows for easy addition of new data types (e.g., lab results, imaging) without disrupting existing flows.

--- Future (What Can Be Done More) ---
- Post-Operation Analytics: Develop a module to export medical logs for post-action reviews and medical research.
- Advanced Query Engine: Implement complex filtering for the Hospital View (e.g., "Show all patients with HR > 120 and Blood Type O-").
- Automated Triage Scoring: Integrate algorithms (like RTS - Revised Trauma Score) that automatically calculate triage levels based on incoming vitals and injury descriptions.

--- Technical Implementation Details ---

Files & Functions Implemented by Member 2:

1. `tactical_ui/models.py`
   - `class MedicalEntry(models.Model)`: The core data structure for persisting medical logs.
   - `to_dict()`: Helper method for seamless JSON serialization.

2. `tactical_ui/views.py`
   - `transform_backend_data(backend_data)`: The "Brain" function that merges real-time vitals with DB records.
   - `api_soldiers_view(request)`: Main GET endpoint for the frontend.
   - `api_add_medical_entry_view(request)`: Handles the persistence of new medical entries.

--- Core Logic Code Snippets ---

Backend - Data Model (`tactical_ui/models.py`):
```python
class MedicalEntry(models.Model):
    service_number = models.CharField(max_length=50, db_index=True)
    injury = models.CharField(max_length=200, default='Assessment')
    body_part = models.CharField(max_length=100, default='Unknown')
    treatment = models.CharField(max_length=500, blank=True, default='')
    confirmed = models.BooleanField(default=False)
    timestamp = models.DateTimeField(auto_now_add=True)
```

Backend - Data Merging Logic (`tactical_ui/views.py`):
```python
def transform_backend_data(backend_data):
    soldiers = []
    for soldier_data in backend_data:
        service_number = soldier_data.get('service_number', 'UNKNOWN')
        # Fetch persistent logs from DB
        logs = list(MedicalEntry.objects.filter(service_number=service_number).values()[:20])
        # Merge with real-time vitals
        soldiers.append({
            'id': service_number,
            'triage': soldier_data.get('status', 'GREEN'),
            'hr': soldier_data.get('heart_rate', 75),
            'logs': logs,
            # ... other fields
        })
    return soldiers
```
